仮想環境の構築のやり方


# venvとは
venvはpythonの仮想環境を構築するためのツール

## この記事のまとめ
- `venv` は Python 3.3 以降で標準搭載されている仮想環境作成ツール
- 各プロジェクトごとに独立した Python 環境を作ることで、パッケージのバージョン衝突を防げる
- `venv` を使うと、本番環境との整合性を保ちつつ、安全にパッケージの試験や開発ができる

## 基本的な使い方
### 仮想環境の作成
- Windows: `python -m venv my_project`
- macOS / Linux: `python3 -m venv my_project`

### 仮想環境の有効化
- Windows (コマンドプロンプト): `my_project\Scripts\activate.bat`
- Windows (PowerShell): `my_project\Scripts\Activate.ps1`
- macOS / Linux: `source my_project/bin/activate`

### 無効化
- `deactivate`

## パッケージ管理
- 仮想環境を有効にした状態で `pip install package_name` を実行すると、その環境内だけにパッケージがインストールされる
- 例: `pip install requests`, `pip install numpy==2.2.0`, `pip install matplotlib pandas`

## requirements.txt の活用
- `pip freeze > requirements.txt` で現在の環境の依存パッケージを保存
- `pip install -r requirements.txt` で他の環境に同じパッケージ構成を再現できる
- 例: `Django==4.2.20`, `requests==2.32.3`, `numpy==1.26.0`

## venvの仕組み（深い理解）

### 仮想環境の構成要素
venv が作成するディレクトリ構造は3つの層で構成される：
1. **ストレージ上**: `venv/` フォルダ群として物理的に存在する
2. **シェル環境上**: `PATH` 環境変数を変更することで仮想環境を「使う」状態にする
3. **メモリ上**: Python を実行するとき、仮想環境の子プロセスがメモリに展開される

### venv/bin/ 配下の主要ファイル
- `activate`: シェルスクリプト（bash/zsh用）で、環境変数を設定して仮想環境を有効化する
- `python` / `python3`: 仮想環境専用の Python 実行ファイル（実体またはシンボリックリンク）
- `pip` / `pip3`: 仮想環境専用のパッケージ管理ツール

### activate スクリプトの動作メカニズム
`source venv/bin/activate` を実行すると、以下の処理が現在のシェルプロセスで行われる：

1. **PATH 環境変数の変更**
   ```
   _OLD_VIRTUAL_PATH="$PATH"
   VIRTUAL_ENV="/home/user/project/venv"
   PATH="$VIRTUAL_ENV/bin:$PATH"
   ```
   仮想環境の `bin/` ディレクトリを PATH の先頭に追加する。これにより、以後の `python3` や `pip3` コマンドはグローバル環境ではなく仮想環境内のものを指すようになる。

2. **VIRTUAL_ENV 環境変数の定義**
   ```
   VIRTUAL_ENV="/home/user/project/venv"
   export VIRTUAL_ENV
   ```
   現在どの仮想環境が有効になっているかを示す。deactivate 時にも使用される。

3. **シェルプロンプトの変更（任意）**
   ```
   PS1="(venv) $PS1"
   ```
   仮想環境が有効であることを視覚的に示すため、プロンプトに環境名を表示する。

4. **deactivate 関数の定義**
   ```
   deactivate () {
       PATH="$_OLD_VIRTUAL_PATH"
       export PATH
       unset VIRTUAL_ENV
       unset deactivate
   }
   ```
   仮想環境を終了させるための関数が追加される。

### source コマンドと外部コマンドの違い
- **source コマンド**: ビルトインコマンドで、現在のシェルプロセス内で実行される（新しいプロセスを生成しない）
- **外部コマンド**: 新しいシェルプロセスを生成して実行される（fork → exec の流れ）

`source venv/bin/activate` が重要なのは、現在のシェルプロセスに環境変数の変更を反映させるため。もし新しいプロセスで実行したら、親プロセスの環境変数は変わらないままになる。

### activate と deactivate の本質
- **activate 前**:  `PATH = /usr/local/bin:/usr/bin:/bin:...`
- **activate 後**:  `PATH = /home/user/project/venv/bin:/usr/local/bin:/usr/bin:/bin:...`
- **deactivate 後**: `PATH = /usr/local/bin:/usr/bin:/bin:...`（元に戻る）

### 仮想環境内でのプロセス実行
仮想環境を有効にした状態で `python3 script.py` を実行すると：
1. PATH の先頭に仮想環境の `bin/` があるため、仮想環境内の `python3` が検索される
2. その `python3` がプロセスとしてメモリにロードされ、スクリプトを実行する
3. インストールされるパッケージはすべて `venv/lib/pythonX.X/site-packages/` に保存される

## まとめ
- `venv` は追加インストール不要で手軽に始められる
- プロジェクトごとの依存関係を分離し、開発効率と環境の再現性を向上させる
- 仮想環境は「ストレージ上のフォルダ」+「環境変数の変更」+「プロセス実行の原則」で成り立っている
- activate は単なる環境変数の書き換えに過ぎず、OSやシェル、ファイルシステム、プロセスといった複数の概念が密接に連動して動作する
- まずは `python -m venv` → `activate` → `pip install` を試すことがおすすめ